name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 30
    runs-on: ubuntu-latest

    env:
      SLEEKFLOW_EMAIL: ${{ secrets.SLEEKFLOW_EMAIL }}
      SLEEKFLOW_PASSWORD: ${{ secrets.SLEEKFLOW_PASSWORD }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Check required environment variables
        run: |
          test -n "$SLEEKFLOW_EMAIL" || (echo "SLEEKFLOW_EMAIL is missing" && exit 1)
          test -n "$SLEEKFLOW_PASSWORD" || (echo "SLEEKFLOW_PASSWORD is missing" && exit 1)
          echo "Required secrets are available"

      - name: Run Playwright tests
        run: npm run test:chromium

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30